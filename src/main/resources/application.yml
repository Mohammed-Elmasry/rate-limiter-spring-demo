spring:
  application:
    name: rate-limiter

  datasource:
    url: jdbc:postgresql://localhost:5432/ratelimiter
    username: root
    password: ratelimiter
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=300s
    cache-names:
      - policies
      - policyByName
      - tenants
      - ipRules
      - apiKeys
      - ipPolicies

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when_authorized
    prometheus:
      enabled: true
  health:
    circuitbreakers:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
      slo:
        http.server.requests: 50ms,100ms,200ms,500ms,1s

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
        recordExceptions:
          - org.springframework.data.redis.RedisConnectionFailureException
          - org.springframework.data.redis.RedisSystemException
          - java.io.IOException
          - java.util.concurrent.TimeoutException
    instances:
      redis:
        baseConfig: default
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 5s
        failureRateThreshold: 50
      database:
        baseConfig: default
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 10s
        failureRateThreshold: 50
        recordExceptions:
          - org.springframework.dao.DataAccessException
          - jakarta.persistence.PersistenceException
          - java.sql.SQLException
  retry:
    configs:
      default:
        maxAttempts: 3
        waitDuration: 100ms
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
        retryExceptions:
          - org.springframework.data.redis.RedisConnectionFailureException
          - org.springframework.dao.TransientDataAccessException
    instances:
      redis:
        baseConfig: default
        maxAttempts: 3
        waitDuration: 100ms
      database:
        baseConfig: default
        maxAttempts: 2
        waitDuration: 200ms

server:
  port: 8080

# Alerting Configuration
alerting:
  enabled: true
  check-interval: 60s
  initial-delay: 30s
  notifiers:
    slack:
      enabled: false
      webhook-url: ${SLACK_WEBHOOK_URL:}
    email:
      enabled: false
      from: alerts@ratelimiter.com
      to: ${ALERT_EMAIL_TO:ops@company.com}
    webhook:
      enabled: false
      url: ${ALERT_WEBHOOK_URL:}

# Email Configuration (for email notifier)
# Uncomment and configure if using email notifications
#  mail:
#    host: smtp.gmail.com
#    port: 587
#    username: ${MAIL_USERNAME:}
#    password: ${MAIL_PASSWORD:}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true

---
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/ratelimiter

  data:
    redis:
      host: redis

---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:postgresql://localhost:5432/ratelimiter_test

  flyway:
    clean-disabled: false
